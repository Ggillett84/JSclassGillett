const extraMoney = true;
const openTime = 4;
const kids = false;
const carWorking = "yes";
// extraMoney ()
// openTime ()
// kids ()

if (kids === false && openTime <= 4) {
  if (extraMoney === true && carWorking === "yes") {
    console.log(`We are going to Sakanaya`);
  } else {
    console.log(`We are going to Wasabi on Fire`);
  }
} else {
  console.log(`We are staying home today`);
}
